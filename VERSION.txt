mvp-iter2.2.125-contour-toggle-immediate
- UX: тумблер "Скрыть контур" теперь применяется сразу в любом режиме (раньше реально скрывал только в режиме "Просмотр" и мог сам включаться обратно по клику).
- UX: кнопка "Просмотр" автоматически скрывает контур (чистый превью-рендер без лишних оверлеев).
- Логика контура/вырезов/экспорт PNG и hard-clip/inward-AA не изменялись.
- Build stamp updated + cache-bust query params updated (iter2_2_125_0)
