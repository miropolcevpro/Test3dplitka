mvp-iter2.2.122-calib-bpair-fallback
- Fix: 3D-калибровка по контуру/линиям больше не падает с ошибкой (B_pair_near_parallel).
  Если линии B (поперёк укладки) почти параллельны в кадре (vanishing at infinity),
  применяется безопасный fallback «A-only» — используется только ось A для авто Horizon/Perspective.
  Это убирает ложные ошибки, сохраняет текущую логику и позволяет продолжать работу без ручных костылей.
- UI: при fallback показывается предупреждение (warn), но статус остаётся READY.
- Build stamp updated + cache-bust query params updated (iter2_2_122_0)
