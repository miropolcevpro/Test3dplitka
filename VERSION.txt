mvp-iter2.2.133-reset-zone-rearm-v2

Fix: Reset зоны полностью "перевооружает" режим построения контура.

- После «Сброс зоны» теперь всегда можно сразу заново ставить точки контура на той же фотографии.
- Принудительно показывается контур (если ранее был скрыт тумблером), иначе создавалось ощущение, что точки не ставятся.
- Сбрасываются состояния интеракции редактора (selectedPoint/draggingPoint/isPointerDown), чтобы не зависать в старом режиме.
- 3D‑калибровка и базовая логика сброса зоны остаются как в предыдущем патче (без регрессий).

Cache-bust: iter2_2_133_0
